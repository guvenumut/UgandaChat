<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🔥 UgandaChat - Ana Sayfa</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 20px;
        }

        .hero-content {
            text-align: center;
            color: white;
            max-width: 800px;
        }

        .fire-logo {
            font-size: 6rem;
            background: linear-gradient(45deg, #ff5722, #ff9800);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 30px;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        .hero h1 {
            font-size: 4rem;
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            font-weight: 700;
        }

        .hero p {
            font-size: 1.4rem;
            margin-bottom: 40px;
            opacity: 0.9;
            line-height: 1.6;
        }

        .cta-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
            margin-bottom: 60px;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 15px 30px;
            border: none;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            min-width: 180px;
            justify-content: center;
        }

        .btn-primary {
            background: linear-gradient(45deg, #ff5722, #ff9800);
            color: white;
            box-shadow: 0 4px 15px rgba(255, 87, 34, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(255, 87, 34, 0.4);
        }

        .btn-secondary {
            background: transparent;
            color: white;
            border: 2px solid white;
        }

        .btn-secondary:hover {
            background: white;
            color: #667eea;
            border-color: white;
        }

        .features {
            background: white;
            padding: 80px 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .features h2 {
            text-align: center;
            font-size: 2.5rem;
            color: #333;
            margin-bottom: 60px;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
        }

        .feature-card {
            text-align: center;
            padding: 40px 20px;
            border-radius: 20px;
            background: #f8f9fa;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
        }

        .feature-card i {
            font-size: 3.5rem;
            color: #ff5722;
            margin-bottom: 20px;
        }

        .feature-card h3 {
            font-size: 1.5rem;
            margin-bottom: 15px;
            color: #333;
        }

        .feature-card p {
            color: #666;
            line-height: 1.6;
        }

        .stats {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 60px 20px;
            color: white;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 40px;
            text-align: center;
        }

        .stat-item h3 {
            font-size: 3rem;
            margin-bottom: 10px;
            color: #ff9800;
        }

        .stat-item p {
            font-size: 1.1rem;
            opacity: 0.9;
        }

        .footer {
            background: #333;
            color: white;
            text-align: center;
            padding: 40px 20px;
        }

        .footer p {
            margin: 0;
            opacity: 0.8;
        }

        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2.5rem;
            }
            
            .fire-logo {
                font-size: 4rem;
            }
            
            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }
            
            .btn {
                width: 250px;
            }
        }
    </style>
</head>
<body>
    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <i class="fas fa-fire fire-logo"></i>
            <h1>UgandaChat</h1>
            <p>
                Gerçek zamanlı mesajlaşma deneyimi yaşa! <br>
                Modern, güvenli ve hızlı chat uygulaması.
            </p>
            
            <div class="cta-buttons">
                <a href="/register" class="btn btn-primary">
                    <i class="fas fa-user-plus"></i>
                    Hemen Başla
                </a>
                <a href="/login" class="btn btn-secondary">
                    <i class="fas fa-sign-in-alt"></i>
                    Giriş Yap
                </a>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features">
        <div class="container">
            <h2>Neden UgandaChat?</h2>
            <div class="features-grid">
                <div class="feature-card">
                    <i class="fas fa-bolt"></i>
                    <h3>Anlık Mesajlaşma</h3>
                    <p>Socket.io teknolojisi ile gerçek zamanlı, kesintisiz mesajlaşma deneyimi.</p>
                </div>
                
                <div class="feature-card">
                    <i class="fas fa-shield-alt"></i>
                    <h3>Güvenli Altyapı</h3>
                    <p>Güvenli kullanıcı kimlik doğrulama sistemi ve veri koruması.</p>
                </div>
                
                <div class="feature-card">
                    <i class="fas fa-users"></i>
                    <h3>Oda Sistemi</h3>
                    <p>Arkadaşlarınla özel odalar oluştur, grup sohbetleri düzenle.</p>
                </div>
                
                <div class="feature-card">
                    <i class="fas fa-mobile-alt"></i>
                    <h3>Responsive Tasarım</h3>
                    <p>Masaüstü, tablet ve mobil cihazlarda mükemmel kullanım deneyimi.</p>
                </div>
                
                <div class="feature-card">
                    <i class="fas fa-cloud"></i>
                    <h3>Cloud Storage</h3>
                    <p>Tüm mesajların bulutta güvenli şekilde saklanması ve senkronizasyonu.</p>
                </div>
                
                <div class="feature-card">
                    <i class="fas fa-paint-brush"></i>
                    <h3>Modern UI</h3>
                    <p>Kullanıcı dostu, modern ve sezgisel arayüz tasarımı.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Stats Section -->
    <section class="stats">
        <div class="container">
            <div class="stats-grid">
                <div class="stat-item">
                    <h3 id="userCount">0</h3>
                    <p>Aktif Kullanıcı</p>
                </div>
                <div class="stat-item">
                    <h3 id="messageCount">0</h3>
                    <p>Gönderilen Mesaj</p>
                </div>
                <div class="stat-item">
                    <h3 id="roomCount">0</h3>
                    <p>Oluşturulan Oda</p>
                </div>
                <div class="stat-item">
                    <h3>24/7</h3>
                    <p>Kesintisiz Hizmet</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 UgandaChat - Made with ❤️ by Kanya</p>
        </div>
    </footer>

    <script>
        function animateCounter(element, target) {
            let current = 0;
            const increment = target / 50;
            const timer = setInterval(() => {
                current += increment;
                if (current >= target) {
                    current = target;
                    clearInterval(timer);
                }
                element.textContent = Math.floor(current);
            }, 30);
        }

        window.addEventListener('load', () => {
            animateCounter(document.getElementById('userCount'), 150);
            animateCounter(document.getElementById('messageCount'), 2500);
            animateCounter(document.getElementById('roomCount'), 45);
        });

        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    </script>
</body>
</html> 